# Activity Log service

Migrated from **GkActivityLogService**. Mounted at `/activity-log`.

## Files
`models.py` (ActivityLog) · `schemas.py` · `controller.py` · `router.py`

## Entities & endpoints
Standard CRUD on `/activity-log/logs` (search: module, feature, action).

## Tests
`tests/test_remaining_services.py` — `test_activity_logs`.
